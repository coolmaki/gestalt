{
  "private": true,
  "packageManager": "pnpm@10.33.2",
  "engines": {
    "pnpm": ">=10.0.0",
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r test"
  }
}
